{
  "name": "cron-worker-threads",
  "version": "1.0.0",
  "description": "NodeJS library for starting cron jobs in separate threads",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "prepublish": "tsc",
    "start:example": "nodemon example/example.ts",
    "publish": "npm publish"
  },
  "keywords": ["worker_threads", "cron"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.9.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.7",
    "@types/uuid": "^9.0.1"
  }
}
